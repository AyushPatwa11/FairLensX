# Optional: Gemini API for enhanced text analysis and AI chat
# GEMINI_API_KEY is removed from source for security. Set this in Render dashboard or local .env (not committed).
GEMINI_API_KEY=

# Server config
HOST=0.0.0.0
PORT=8000
